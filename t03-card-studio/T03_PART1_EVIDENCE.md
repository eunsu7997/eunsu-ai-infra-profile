# T03 Part 1 조건 검증 — PIXEL CARD STUDIO

검증 범위: **T03-C03 ~ T03-C16**  
검증 환경: headless Chromium 1440×1000 + 실제 Canvas PNG 다운로드 비교  
결과: **14/14 PASS, Console/Page error 0**

## C03 — 첫 화면에 이미지 편집 + 문구 편집 도구
- 1440×1000 viewport에서 이미지 도구 시작 y=123, 문구 도구 시작 y=500.5.
- 두 편집 도구가 첫 화면에 노출됨.
- **PASS**

## C04 / C05 — PNG / JPEG 불러오기
- PNG: `portrait.png` 700×1200 정상 로드.
- JPEG: `landscape.jpg` 1200×700 정상 로드.
- **PASS**

## C06 / C07 / C08 — 문구 위치·크기·색 즉시 반영
- 위치: x 50%/y 78% → x 23%/y 31% 즉시 반영.
- 크기: 92px 즉시 반영.
- 색: `#12abef` 즉시 반영.
- **PASS**

## C09 / C10 — 잘못된 파일 거부 + 작업 유지 + 이유 표시
- 손상 PNG를 넣었을 때 이전 이미지/문구/위치/크기 유지.
- 화면 메시지: `이미지를 해석하지 못했습니다. 손상되지 않은 PNG 또는 JPEG를 사용해 주세요. 기존 편집 내용은 유지했습니다.`
- **PASS**

## C11 / C12 / C13 — 1:1 / 4:5 / 9:16 미리보기 ↔ 다운로드 일치
동일 편집 상태(`RATIO TEST\n줄바꿈 유지`, x61%, y72%, 70px)에서 각 비율을 저장해 Canvas 미리보기 PNG와 다운로드 파일을 픽셀 비교함.

- 1:1: 1080×1080 / pixel match `true`
- 4:5: 1080×1350 / pixel match `true`
- 9:16: 1080×1920 / pixel match `true`
- 세 비율 모두 줄바꿈 결과 `RATIO TEST` / `줄바꿈 유지` 동일.
- **PASS**

## C14 — 극단 입력 12건
1. 긴 한글
2. 긴 영문 무공백 문자열
3. 한글+영문 혼합
4. 명시적 3줄 줄바꿈
5. 이모지 포함
6. 빈 문구
7. 최소 글자 크기 18
8. 최대 글자 크기 180
9. 세로 이미지
10. 가로 이미지
11. 투명 PNG
12. 극단 문구 위치 x5% / y95%

12건 모두 렌더링/상태 보존 검사 **PASS**.

## C15 — 수정 전 FAIL → 같은 입력 수정 후 PASS
실제로 발견한 버그를 기록함.

- 같은 입력: 기본 문구 `오늘의 한 장` + 줄바꿈 + `MAKE IT YOURS.`
- 수정 전: HTML textarea 기본값에 실제 개행 대신 문자 `\\n`이 들어가 한 줄에서 `\n`이 보임 → **FAIL**
- 수정: textarea 안에 실제 개행 문자로 교체.
- 수정 후: 상태값이 실제 `\n`을 가진 2줄 문구로 렌더링 → **PASS**

## C16 — 잘못된 극단 입력 뒤 기존 편집 내용 유지
검사 직전 상태:
- 문구 `C16 KEEP`
- 크기 77
- 위치 x44% / y66%
- 이미지 `landscape.jpg` 1200×700

`bad.txt` 입력 뒤 위 상태가 모두 동일하게 유지되었고 거부 이유가 표시됨.
- **PASS**

## Part 1 최종 상태
- **C03~C16: 전부 PASS**
- Console error: **0**
- Page error: **0**
- 다음 범위: C17~C32 템플릿/JSON/완성 이미지/공개 안전성/제출 문구
