# T03 Part 2 조건 검증 — PIXEL CARD STUDIO

검증 범위: **T03-C17 ~ T03-C24**  
구현 범위: 템플릿 CRUD · 새로고침 유지 구조 · JSON 내보내기/가져오기 · 잘못된 JSON 안전 거부  
자동 회귀검사 결과: **C17~C24 8/8 PASS, Console/Page error 0**

> 실제 배포 환경에서는 템플릿을 **IndexedDB**에 저장합니다. 현재 실행 샌드박스는 로컬/가상 URL의 브라우저 저장소 접근을 제한하므로 자동검사에서는 동일 CRUD 로직의 메모리 fallback을 사용해 DOM 재로딩 후 유지 동작까지 재현했습니다. 최종 GitHub Pages 공개본에서는 실제 브라우저 새로고침 유지 여부를 한 번 더 수동 확인합니다.

## C17 — 사용자 템플릿 3개 이상 생성
자동 검사에서 아래 3개를 각각 다른 편집 상태로 생성했습니다.
- `A · SQUARE` — 1:1 / landscape.jpg / `TEMPLATE A`
- `B · FEED` — 4:5 / landscape.jpg / `TEMPLATE B`
- `C · STORY` — 9:16 / portrait.png / `TEMPLATE C`

결과: 저장 템플릿 수 **3개** 확인.  
**PASS**

## C18 — 생성한 템플릿 다시 불러오기
편집 상태를 일부러 `DISTURBED` / 9:16으로 바꾼 뒤 `A · SQUARE`를 불러왔습니다.
- 문구: `TEMPLATE A`
- 비율: `1:1`
- 이미지: `landscape.jpg`

세 값이 저장 당시 상태로 복원됨.  
**PASS**

## C19 — 생성한 템플릿 수정
`B · FEED`를 불러와 다음처럼 수정 후 저장했습니다.
- 이름: `B · FEED MOD`
- 문구: `TEMPLATE B · MODIFIED`
- 문구 크기: `88`

다른 템플릿을 불러온 뒤 다시 B를 불러와 수정 결과가 유지되는 것을 확인했습니다.  
**PASS**

## C20 — 생성한 템플릿 삭제
`C · STORY`를 삭제했습니다.
- 삭제 전: 3개
- 삭제 후: 2개
- 삭제한 ID가 목록에 남지 않음

**PASS**

## C21 — 템플릿 변경 결과가 새로고침 뒤 유지
수정된 `B · FEED MOD`와 삭제된 `C · STORY` 상태를 DOM 재로딩 뒤 다시 확인했습니다.
- B의 수정 이름/문구/88px 유지
- C는 목록에 다시 나타나지 않음

앱의 실제 배포 경로는 IndexedDB이며, 최종 공개 GitHub Pages에서 실제 새로고침 1회 수동 재확인 예정.  
**자동 회귀 PASS / 공개본 최종 재확인 예정**

## C22 — 정상 JSON 가져오기 → 템플릿 복원
다음 편집 상태를 JSON 패키지로 만든 뒤 화면을 다른 상태로 바꾸고 다시 가져왔습니다.
- 이름: `JSON NORMAL`
- 문구: `JSON RESTORE` + 줄바꿈 + `정상 복원`
- 비율: `9:16`
- 글자 크기: `99`
- 이미지: `transparent.png`

가져오기 후 위 상태가 모두 복원되고 템플릿 목록에도 새 항목이 생성되었습니다.  
**PASS**

## C23 — 손상 JSON 거부 + 기존 표시 유지
손상된 JSON 문자열(`{"schema":`)을 가져오기 시도했습니다.
- JSON parse 단계에서 거부
- 현재 문구/비율/글자 크기/이미지 유지
- 저장된 템플릿 개수 유지

**PASS**

## C24 — 필수 항목 누락 JSON 거부 + 저장 전 차단
문법은 정상이나 `template.editor.caption`이 빠진 JSON을 가져오기 시도했습니다.
- 메시지: `필수 항목이 없습니다: caption`
- 현재 화면 상태 유지
- 템플릿 개수 유지
- 잘못된 데이터가 저장소에 추가되지 않음

**PASS**

## Part 1 회귀검사
Part 2 기능을 추가한 뒤 기존 `T03-C03~C16` 자동검사를 다시 실행했습니다.
- **14/14 PASS**
- Console/Page error **0**

즉 Part 2 추가로 Part 1 기능이 깨지지 않았습니다.

## Part 2 종료 상태
- **C17~C20, C22~C24: 자동검사 PASS**
- **C21: 자동 회귀 PASS + 최종 공개 URL 실제 새로고침 1회만 남음**
- 다음 범위: **C25~C32** — 완성 이미지 3개 / 출처·제작 방식 / 메타데이터·개인정보·비밀값 / 제출 문구
