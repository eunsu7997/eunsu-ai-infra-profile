# T02 최종 조건 검증 기록 — RAID//ZERO

검증 기준 빌드: **28초 / HP 5 / 3 Phase / 기본 패턴 간격 0.75초 / 대시 후 0.5초 무적 / 실드 코어 1.5초 무적**

> 과제 화면의 조건 번호는 **C01 + C03~C31 = 총 30개**입니다. C02는 과제 화면에 없습니다.
> 난이도 비교는 실드 코어와 대시 무적을 포함한 동일 gameplay에서 기본 패턴 간격 한 값만 바꿔 실시했습니다.

## 카드 1 — 핵심 규칙

### C01 — 결과/소스 URL 무로그인 공개
- 결과 URL: `https://eunsu7997.github.io/eunsu-ai-infra-profile/t02-mini-game/`
- 소스 저장소: `eunsu7997/eunsu-ai-infra-profile`
- 저장소 Public 확인 및 사용자 InPrivate 무로그인 확인 완료.
- **PASS**.

### C03 — 게임 규칙 표시
- 오른쪽 `핵심 규칙`에 장판 회피, HP/실패, 실드 코어 1.5초 무적, 대시 직후 0.5초 무적, 28초 성공/S등급 조건을 표시.
- **PASS**.

### C04 — 핵심 조작 표시
- WASD/방향키 이동, Space 대시(+0.5초 무적), P 일시정지, R 결과 화면 재시작을 상시 표시.
- **PASS**.

### C05 — 현재 게임 상태 표시
- 남은 시간, Phase, HP, Score, Combo, Dash, Shield, State 표시.
- **PASS**.

### C06 — 핵심 입력 1회 = 핵심 액션 1회
- Space `keydown` 1회당 dash 처리 1회, `event.repeat` 무시.
- 최종 0.75초 빌드 자동검사에서 Space 10회 → `coreInputEvents +10`, `coreActions +10` 확인.
- **PASS**.

### C07 — 시작 후 30초 안에 결과 도달
- `ROUND_SECONDS=28`.
- HP 0이면 즉시 실패, 28초 생존 시 성공.
- **PASS**.

### C08 / C09 — 성공·실패 후 재시작 초기화
- 새 판은 28초 / HP5 / Score0 / Combo0 / Phase1 / 플레이어 `(480,385)`에서 시작하도록 `fresh()`로 상태 재생성.
- 기존 자동 reset 검사에서 성공/실패 양쪽 초기화 확인.
- **PASS**.

## 카드 2 — PC 경계/안정성

### C10 — 1366×768
- 최종 0.75초 빌드 Chromium 재검사: horizontal overflow `0`, 시작 모달 viewport 내부, console/page error 0.
- **PASS**.

### C11 — 1920×1080
- 최종 0.75초 빌드 Chromium 재검사: horizontal overflow `0`, 시작 모달 viewport 내부, console/page error 0.
- **PASS**.

### C12 — 1초 안에 동일 핵심 입력 10회
- 최종 0.75초 빌드에서 Space 10회 → input 10 / action 10.
- **PASS**.

### C13 — 창 크기 변경 시 상태/조작 유지
- resize 이벤트는 UI 동기화만 수행하며 game state를 재생성하지 않음.
- 기존 Chromium viewport 변경 검사에서 상태/조작 유지 확인.
- **PASS**.

### C14 — 포커스 이탈/복귀
- blur 시 자동 pause, focus 시 같은 판 resume.
- 기존 Chromium 검사에서 pause 동안 elapsed 변화 0 및 복귀 후 이동 확인.
- **PASS**.

### C15 — 일시정지 중 진행 멈춤
- 최종 0.75초 빌드 빠른 회귀검사에서 pause 중 elapsed 변화 `0.0` 확인.
- **PASS**.

### C16 / C17 — 최종 빌드 10분 연속 실행
- 기능 추가 전 빌드는 실제 Chromium **600.68초** 연속 실행 및 Console/Page error 0을 확인했음.
- 이후 **실드 코어 + 대시 0.5초 무적 + 최종 0.75초 간격**으로 변경되었으므로, 제출 직전 최종 공개 URL 기준 10분 연속 검사를 다시 1회 실시해야 함.
- 확인 항목: 10분 뒤 이동/대시/실드 획득 가능 + DevTools Console red error 0.
- **상태: 최종 수동 재확인 필요**.

## 카드 3 — 10회 전후 난이도 선택

동일한 최종 gameplay(28초, HP5, 대시 0.5초 무적, 실드 코어 1.5초 무적, **축소된 Phase 3 추적 도넛 포함**, 같은 이동속도/대시 쿨다운)를 유지하고 **기본 패턴 간격 `INT` 한 값만** 650ms와 750ms로 바꿔 동일 seed 101~110 자동 QA를 실시했습니다.

### C18 — 변경 전 10회 기록 (0.65초)

| 회차 | 결과 | 생존시간(s) | 종료 HP | 점수 | 회피 |
|---:|---|---:|---:|---:|---:|
|1|성공|28.0|3|12201|166|
|2|실패|27.5|0|9936|148|
|3|실패|22.4|0|6458|96|
|4|성공|28.0|4|12398|161|
|5|실패|25.8|0|7601|119|
|6|성공|28.0|3|12774|172|
|7|성공|28.0|3|12421|168|
|8|성공|28.0|2|10661|156|
|9|실패|25.0|0|6745|107|
|10|성공|28.0|1|10413|152|

- 성공 **6 / 10**
- 생존 범위 **22.4~28.0초**
- 생존 중앙값 **28.0초**
- **PASS — 10회 기록 확보**.

### C19 — 변경 후 10회 기록 (0.75초, 최종)

| 회차 | 결과 | 생존시간(s) | 종료 HP | 점수 | 회피 |
|---:|---|---:|---:|---:|---:|
|1|실패|25.5|0|6942|114|
|2|성공|28.0|1|8025|123|
|3|실패|27.5|0|6924|105|
|4|성공|28.0|1|8564|129|
|5|성공|28.0|2|8541|125|
|6|성공|28.0|1|9337|137|
|7|실패|27.5|0|7221|115|
|8|실패|24.0|0|6095|93|
|9|실패|25.5|0|6609|109|
|10|성공|28.0|2|7871|115|

- 성공 **5 / 10**
- 생존 범위 **24.0~28.0초**
- 생존 중앙값 **27.75초**
- **PASS — 10회 기록 확보**.

> 같은 자동 입력에서는 성공 횟수가 6→5로 줄어 단순히 “0.75초가 항상 더 쉽다”고 해석하지 않았습니다. 대신 최소 생존시간이 22.4→24.0초로 올라갔고, 실제 플레이에서 사용자가 기존 난도를 어렵게 느껴 완화를 요청한 점을 함께 반영했습니다.

### C20 — 전후 한 개 난이도 값만 변경
- 비교 시 **현재 최종 코드(축소된 Phase 3 추적 도넛 포함)**에서 `INT=650` ↔ `INT=750`만 변경.
- HP, 한 판 시간, 이동속도, 대시 거리/쿨다운, 대시 0.5초 무적, 실드 코어 1.5초 무적, 패턴 로직과 도넛 크기는 동일.
- **PASS**.

### C21 — 최종 난이도 선택 근거
- 자동 QA의 성공 수는 0.65초 6/10, 0.75초 5/10으로 큰 차이가 없고 0.75초가 모든 지표에서 우세하지는 않았습니다.
- 다만 최소 생존시간은 **22.4초 → 24.0초**로 개선됐고, 사용자가 직접 플레이에서 기존 난도를 어렵게 느껴 완화를 요청했습니다.
- 패턴 빈도를 낮춰 반응 여유를 주되 Phase 3 복합 패턴은 유지하는 **0.75초를 최종 선택**했습니다.
- **PASS**.

## 카드 4 — 저장/복구

### C22 — 새 게임 초기화
- ready 상태: 28초 / Phase1 / HP5 / Score0 / Combo0 / Dodge0 / cooldown0 / `(480,385)` / hazards0.
- **PASS**.

### C23 — 선언한 기록 reload 보존
- 보존 대상: `wins`, `bestScore`, `bestTime`.
- 테스트값 `{wins:3,bestScore:4321,bestTime:28}` 로드/재로드 확인.
- **PASS**.

### C24 — 빈 storage 기본값
- `{wins:0,bestScore:0,bestTime:0}`으로 정상 시작.
- **PASS**.

### C25 — 손상 storage 복구
- 깨진 JSON은 catch 후 `{0,0,0}` 기본값으로 복구.
- **PASS**.

## 카드 5 — 효과/아이템/안전/제출 형식

### 실드/대시 회귀검사
- 최종 0.75초 빌드 config: `ATTACK_INTERVAL_MS=750`, `DASH_INVULN=0.5`, `SHIELD_INVULN=1.5`.
- Space 직후 `invuln=0.5` 확인.
- 실드 코어 등장 확인, 획득 직후 `shieldTime=1.5` 확인.
- Phase 3 추적 도넛 크기: inner `50`, outer `120`으로 축소.
- **PASS**.

### C26 — 선택 효과가 선언된 이벤트에서만 실행
- 화면 선언: 대시·피격·페이즈 전환·실드 코어 획득·클리어/실패 이벤트에서 파티클 사용.
- **PASS**.

### C27 — 효과 줄이기 즉시 적용
- 줄이기 ON 시 현재 particles 제거, screenShake 0, dashFx/clearFx 제거.
- 게임 판정(대시/실드 무적)은 유지.
- **PASS**.

### C28 — 공개 금지 개인정보 0
- 게임/제출 파일에 연락처·정확한 위치 등 공개 금지 개인정보를 넣지 않음.
- **PASS**.

### C29 — 비밀값 0
- password/token/API key/private key를 넣지 않음.
- **PASS**.

### C30 — 짧은 확인법 4요소
- README/SUBMISSION_TEXT에 `어디로 / 3단계 이내 행동 / 통과 / 실패`를 분리 작성.
- **PASS**.

### C31 — AI/내 판단 3요소
- README/SUBMISSION_TEXT에 `AI에게 맡긴 일 / 내가 직접 판단한 일 / AI 제안을 따르지 않은 일`을 분리 작성.
- **PASS**.

---

# 최종 상태
- **C01, C03~C15, C18~C31: PASS**
- **C16/C17: 최종 공개 URL 10분 연속 수동 재확인 1회 필요**
- 게임 시간: **28초**
- 기본 패턴 간격: **0.75초 (`INT=750`)**
- 대시 무적: **0.5초**
- 실드 코어: **한 판 1~2회 / 획득 시 1.5초 무적**
- Phase 3 추적 도넛: **inner 50 / outer 120**
