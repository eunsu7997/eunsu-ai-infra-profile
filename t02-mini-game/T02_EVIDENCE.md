# T02 최종 조건 검증 기록 — RAID//ZERO

검증 기준 빌드: **28초 / HP 5 / 3 Phase / 기본 패턴 간격 0.70초 (`ATTACK_INTERVAL_MS = 700`)**

> 과제 화면의 조건 번호는 **C01 + C03~C31 = 총 30개**입니다. C02는 과제 화면에 없습니다.
> 이 문서는 현재 최종 제출 파일 기준으로 조건을 하나씩 다시 확인한 기록입니다.

## 카드 1 — 30초 핵심 규칙

### C01 — 결과/소스 URL 무로그인 공개
- 소스 저장소: `eunsu7997/eunsu-ai-infra-profile`
- GitHub repository visibility: **public 확인 완료**.
- 결과 URL: `https://eunsu7997.github.io/eunsu-ai-infra-profile/t02-mini-game/`
- 소스 URL: `https://github.com/eunsu7997/eunsu-ai-infra-profile/tree/main/t02-mini-game`
- **최종 상태: 사용자 InPrivate 확인 완료.** 결과/소스 두 URL 모두 로그인/초대/비밀번호 없이 열림.

### C03 — 게임 규칙 표시
- 오른쪽 `핵심 규칙`에 3개 규칙을 상시 표시.
- **PASS**.

### C04 — 핵심 조작 표시
- WASD/방향키 이동, Space 대시, P 일시정지, R 결과 화면 재시작을 상시 표시.
- **PASS**.

### C05 — 현재 게임 상태 표시
- 남은 시간, Phase, HP, Score, Combo, Dash, State 상시 표시.
- **PASS**.

### C06 — 핵심 입력 1회 = 핵심 액션 1회
- `keydown` Space 1회당 `coreDashAction()` 1회 호출.
- `event.repeat` 입력은 무시.
- 쿨다운 중에도 입력 처리 1회 후 BLOCKED로 종료하여 중복 액션 없음.
- **PASS**.

### C07 — 시작 후 30초 안에 결과 도달
- `ROUND_SECONDS = 28`.
- HP가 먼저 0이면 즉시 실패, 아니면 28초 생존 시 성공.
- **60초 값 없음 재검사 완료**.
- **PASS**.

### C08 — 성공 후 재시작 초기화
- 자동 런타임 검사에서 성공 후 restart 시 `timeLeft=28`, `hp=5`, `score=0`, `combo=0`, `phase=1`, 플레이어 `(480,385)` 확인.
- `startRound()`이 fresh state를 만든 뒤 초기 추적 예고 패턴 1개를 생성하는 것은 모든 새 판에서 동일한 정상 시작 상태임.
- **PASS**.

### C09 — 실패 후 재시작 초기화
- 자동 런타임 검사에서 실패 후 restart도 성공 후와 동일한 초기값으로 복귀.
- **PASS**.

## 카드 2 — PC 경계 검사

### C10 — 1366×768
- 실제 Chromium viewport 1366×768 검사.
- `innerWidth=1366`, `scrollWidth=1366`, horizontal overflow `0`.
- 시작 모달 전체가 viewport 안에 있음.
- **PASS**.

### C11 — 1920×1080
- 실제 Chromium viewport 1920×1080 검사.
- `innerWidth=1920`, `scrollWidth=1920`, horizontal overflow `0`.
- 시작 모달 전체가 viewport 안에 있음.
- **PASS**.

### C12 — 1초 안에 동일 핵심 입력 10회
- 같은 Space 입력 이벤트 10회 전송.
- `coreInputEvents: +10`, `coreActions: +10` 확인.
- console/page error 0.
- **PASS**.

### C13 — 창 크기 변경 시 상태/조작 유지
- paused 상태에서 viewport를 변경한 전/후 game state 동일.
- 조작 패널 유지.
- **PASS**.

### C14 — 포커스 이탈/복귀
- blur 시 자동 pause, focus 시 같은 판 resume.
- pause 동안 elapsed 변화 0.
- 복귀 후 이동 입력으로 x좌표 변화 확인.
- **PASS**.

### C15 — 일시정지 중 진행 멈춤
- P pause 동안 시간/HP/점수/패턴 상태 정지.
- resume 뒤 같은 판에서 이어짐.
- **PASS**.

### C16 — 10분 연속 실행 뒤 조작 가능
- 자동 Chromium 실제 경과 시간 **600.68초** 연속 실행.
- 30판 시작 / 29판 종료 후에도 이동 조작 정상.
- 종료 후 플레이어 x 좌표 `480 → 557.022` 변화 확인.
- **PASS**.

### C17 — 10분 연속 실행 동안 Console red error 0
- 위 600.68초 검사에서 `console_errors=[]`, `page_errors=[]`.
- **PASS**.

## 카드 3 — 10회 전후 난이도 선택

비교 조건은 현재 최종 코드에서 **`ATTACK_INTERVAL_MS` 한 값만** 바꾼 동일 입력 패턴 자동 QA이다. HP, 이동속도, 대시 거리/쿨다운, 한 판 시간, Phase 패턴 로직은 동일하다.

- 변경 전: **0.65초 (`650ms`)**
- 변경 후/최종: **0.70초 (`700ms`)**
- 동일 seed: 101~110
- 동일 이동/대시 입력 시퀀스 사용

### C18 — 변경 전 10회 기록 (0.65초)

| 회차 | 결과 | 생존시간(s) | 종료 HP | 점수 | 회피 |
|---:|---|---:|---:|---:|---:|
|1|실패|20.7|0|3824|68|
|2|실패|19.4|0|2373|46|
|3|실패|24.6|0|7981|117|
|4|실패|19.8|0|2624|54|
|5|실패|20.7|0|3744|65|
|6|실패|19.3|0|2416|46|
|7|실패|22.4|0|5547|89|
|8|실패|19.4|0|2266|47|
|9|실패|25.1|0|6654|107|
|10|실패|19.4|0|3070|59|

- 생존 범위: **19.3~25.1초**
- 생존 중앙값: **20.25초**
- 점수 범위: **2266~7981**
- 점수 중앙값: **3407**
- **PASS — 10회 기록 확보**.

### C19 — 변경 후 10회 기록 (0.70초)

| 회차 | 결과 | 생존시간(s) | 종료 HP | 점수 | 회피 |
|---:|---|---:|---:|---:|---:|
|1|실패|21.3|0|3780|68|
|2|실패|19.9|0|2912|51|
|3|실패|23.1|0|5820|95|
|4|실패|19.9|0|2106|45|
|5|실패|21.6|0|4199|70|
|6|실패|20.4|0|3692|67|
|7|실패|20.4|0|3799|64|
|8|실패|24.5|0|5626|90|
|9|실패|25.1|0|6278|102|
|10|실패|18.3|0|2640|52|

- 생존 범위: **18.3~25.1초**
- 생존 중앙값: **20.85초**
- 점수 범위: **2106~6278**
- 점수 중앙값: **3789.5**
- **PASS — 10회 기록 확보**.

> 이 기록은 사람 실력 평가가 아니라 두 난이도 값을 같은 입력 조건으로 비교하기 위한 결정적 자동 QA 기록이다.

### C20 — 전후 한 개 난이도 값만 변경
- 비교 harness가 현재 최종 `script.js`에서 `ATTACK_INTERVAL_MS = 700` 한 줄만 650으로 치환하여 비교.
- 다른 gameplay 상수/패턴 로직은 동일.
- **PASS**.

### C21 — 최종 난이도 선택 근거
- 0.65초 생존 중앙값 **20.25초** → 0.70초 **20.85초**로 증가.
- 점수 중앙값도 **3407 → 3789.5**로 증가하여 같은 입력 조건에서 조금 더 진행 가능.
- 직접 플레이에서도 0.65초 버전을 **“좀 빡세다”**고 판단하여 0.70초로 완화.
- 따라서 게임의 긴장감은 유지하면서 한 단계 완화한 **0.70초를 최종 선택**.
- **PASS**.

## 카드 4 — 저장/복구

### C22 — 새 게임 현재값 초기화
- ready 상태: 28초 / Phase1 / HP5 / Score0 / Combo0 / Dodge0 / cooldown0 / `(480,385)` / hazards0 확인.
- **PASS**.

### C23 — 선언한 기록 reload 보존
- 테스트값 `{wins:3,bestScore:4321,bestTime:28}` 저장 후 reload에서 동일 값 확인.
- 실제 보존 대상은 클리어 수, 최고 점수, 최장 생존만.
- **PASS**.

### C24 — 빈 storage 기본값
- storage key 삭제 후 `{wins:0,bestScore:0,bestTime:0}` 정상 시작.
- **PASS**.

### C25 — 손상 storage 복구
- 깨진 JSON 주입 후 `{0,0,0}`으로 복구되고 게임 start 가능.
- **PASS**.

## 카드 5 — 효과/안전/제출 형식

### C26 — 선택 효과가 선언된 이벤트에서만 실행
- 화면 선언: **대시·피격·페이즈 전환·클리어/실패** 때 파티클 발생.
- 코드 `burst()` 호출 위치를 다시 스캔해 위 이벤트와 일치함을 확인.
- **PASS**.

### C27 — 효과 줄이기 즉시 적용
- 효과 ON 테스트 전 particles 52 / dashFx 존재.
- 버튼 클릭 직후 particles 0 / dashFx null / screenShake 0 / clearFx null / `aria-pressed=true`.
- **PASS**.

### C28 — 공개 금지 개인정보 0
- 최종 공개 파일에서 이메일/전화번호 패턴 자동 스캔 0건.
- 정확한 주소/연락처 등 공개하지 않기로 한 개인정보를 넣지 않음.
- **PASS**.

### C29 — 비밀값 0
- password/token/API key/private key 형태 자동 스캔 0건.
- **PASS**.

### C30 — 짧은 확인법 4요소 구분
README에 아래를 분리해 작성.
1. 어디로 가나요
2. 3단계 이내 무엇을 하나요
3. 무엇이 보이면 통과인가요
4. 안 될 때 무엇이 보이나요
- **PASS**.

### C31 — AI/내 판단 3요소 구분
README에 아래를 분리해 작성.
1. AI에게 맡긴 일
2. 내가 직접 판단한 일
3. AI 제안을 따르지 않은 일
- **PASS**.

---

# 최종 상태 요약

- **C01 사용자 InPrivate 무로그인 확인: 완료**
- **자동/코드 검증 PASS: C03~C31 전부**
- 60초 원본 파일은 최종 제출본에서 제외. 최종 게임은 **28초**.
